<?xml version="1.0" encoding="UTF-8" ?>
<project
    xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
>
  <modelVersion>4.0.0</modelVersion>

  <groupId>pt.isel.icd</groupId>
  <artifactId>dotsandboxes</artifactId>
  <version>2026.04.28</version>
  <packaging>war</packaging>

  <name>dotsandboxes</name>
  <url>http://www.example.com</url>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- TP02: alvo JDK 25 (Standard Edition), conforme requisitos do enunciado. -->
    <maven.compiler.release>25</maven.compiler.release>
    <javafx.version>21.0.4</javafx.version>
    <!-- APIs Jakarta EE 11 fornecidas pelo Tomcat 11 (escopo provided). -->
    <jakarta.servlet.version>6.1.0</jakarta.servlet.version>
    <jakarta.websocket.version>2.2.0</jakarta.websocket.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>5.11.0</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-api</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-params</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-controls</artifactId>
      <version>${javafx.version}</version>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-fxml</artifactId>
      <version>${javafx.version}</version>
    </dependency>

    <!--
      APIs do contentor (Tomcat 11 / Jakarta EE 11). Escopo "provided":
      sao usadas para compilar a camada Web mas NAO sao empacotadas no .war,
      pois o Tomcat ja as fornece. Respeita a regra "sem bibliotecas adicionais".
    -->
    <dependency>
      <groupId>jakarta.servlet</groupId>
      <artifactId>jakarta.servlet-api</artifactId>
      <version>${jakarta.servlet.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>jakarta.websocket</groupId>
      <artifactId>jakarta.websocket-api</artifactId>
      <version>${jakarta.websocket.version}</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>



  <build>
    <plugins>
      <plugin>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-maven-plugin</artifactId>
      </plugin>
    </plugins>
    <pluginManagement>
      <plugins>
        <plugin>
          <artifactId>maven-clean-plugin</artifactId>
          <version>3.4.0</version>
        </plugin>
        <plugin>
          <artifactId>maven-resources-plugin</artifactId>
          <version>3.3.1</version>
        </plugin>
        <plugin>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
        </plugin>
        <plugin>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.3.0</version>
        </plugin>
        <plugin>
          <artifactId>maven-jar-plugin</artifactId>
          <version>3.4.2</version>
        </plugin>
        <plugin>
          <artifactId>maven-install-plugin</artifactId>
          <version>3.1.2</version>
        </plugin>
        <plugin>
          <artifactId>maven-deploy-plugin</artifactId>
          <version>3.1.2</version>
        </plugin>
        <plugin>
          <artifactId>maven-site-plugin</artifactId>
          <version>3.12.1</version>
        </plugin>
        <plugin>
          <artifactId>maven-project-info-reports-plugin</artifactId>
          <version>3.6.1</version>
        </plugin>

        <plugin>
          <groupId>org.openjfx</groupId>
          <artifactId>javafx-maven-plugin</artifactId>
          <version>0.0.8</version>
          <configuration>
            <mainClass>pt.isel.icd.ClientApplication</mainClass>
          </configuration>
        </plugin>
        <plugin>
          <artifactId>maven-war-plugin</artifactId>
          <version>3.4.0</version>
          <configuration>
            <!--
              O JavaFX e uma biblioteca externa com modulos nativos: nunca pode
              ser empacotado no .war. Excluimo-lo do WEB-INF/lib (continua
              disponivel para o build da GUI e para "mvn javafx:run").
            -->
            <packagingExcludes>WEB-INF/lib/javafx-*.jar</packagingExcludes>
            <webResources>
              <!--
                Embebe o projeto Maven completo (fonte + pom) dentro do .war,
                em WEB-INF/project, para que o artefacto seja importavel num IDE
                com codigo-fonte editavel. E inerte em runtime (nao servido,
                fora do classpath).
              -->
              <resource>
                <directory>${project.basedir}</directory>
                <targetPath>WEB-INF/project</targetPath>
                <includes>
                  <include>pom.xml</include>
                  <include>src/**</include>
                </includes>
              </resource>
            </webResources>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
