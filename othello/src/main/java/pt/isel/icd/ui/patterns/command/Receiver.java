package pt.isel.icd.patterns.command;

/**
 * Receiver is part of the Command design pattern. Almost any object may act as a receiver that does the actual work.
 */
public interface Receiver {
    
}
