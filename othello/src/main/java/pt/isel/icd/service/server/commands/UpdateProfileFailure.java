package pt.isel.icd.service.server.commands;

import pt.isel.icd.communication.commands.Command;

/**
 * This command is sent when a user fails to update their profile, with the reason for the failure.
 */
public class UpdateProfileFailure implements Command {
}
