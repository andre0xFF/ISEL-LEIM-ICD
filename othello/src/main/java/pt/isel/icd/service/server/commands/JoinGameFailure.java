package pt.isel.icd.service.server.commands;

import pt.isel.icd.communication.commands.Command;

/**
 * This command is sent when a user fails to join a game, with the reason for the failure.
 */
public class JoinGameFailure implements Command {
}
